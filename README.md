# 📘 Excel Refactoring Tool

## 📝 Описание
Этот скрипт предназначен для обработки и рефакторинга данных в Excel-файлах с использованием библиотеки `openpyxl`.

🔹 **Возможности**:
- Вырезание и вставка диапазонов ячеек
- Очистка строк и отдельных ячеек
- Удаление строк и столбцов
- Разъединение и объединение ячеек
- Оптимизация структуры данных в файле

---

## 🚀 Функционал

### ✂ **Вырезание и вставка диапазона**
#### `cut_and_paste_excel(file_path, src_range, dst_cell)`
📌 Вырезает диапазон ячеек и вставляет его в указанную позицию.

**Аргументы:**
- `file_path` — путь к Excel-файлу
- `src_range` — диапазон ячеек (например, "B11:K18")
- `dst_cell` — начальная ячейка для вставки (например, "M5")

---

### 🧹 **Очистка строк и ячеек**
#### `clear_row(file_path, sheet_name, row_num)`
📌 Очищает все ячейки в указанной строке.

#### `clear_cells_in_range(file_path, sheet_name, cell_range)`
📌 Очищает все ячейки в заданном диапазоне.

**Аргументы:**
- `file_path` — путь к Excel-файлу
- `sheet_name` — имя листа
- `row_num` / `cell_range` — номер строки или диапазон ячеек (например, "A1:C3")

---

### ❌ **Удаление строк**
#### `delete_row(file_path, sheet_name, row_number)`
📌 Удаляет строку из Excel-файла.

**Аргументы:**
- `file_path` — путь к Excel-файлу
- `sheet_name` — имя листа
- `row_number` — номер строки для удаления

---

### 🔄 **Объединение и разъединение ячеек**
#### `merge_cells(file_path, cell_range, value=None)`
📌 Объединяет указанный диапазон ячеек и записывает значение.

**Аргументы:**
- `file_path` — путь к Excel-файлу
- `cell_range` — диапазон объединяемых ячеек (например, "A1:C3")
- `value` — значение, которое нужно записать (по умолчанию `None`)

---

### 🏗 **Оптимизация данных**
#### `clean_excel(file_path, output_file, target_col=2)`
📌 Очищает Excel-файл, удаляя нечётные столбцы (кроме первого) и разъединяя объединённые ячейки.

**Аргументы:**
- `file_path` — путь к исходному файлу Excel
- `output_file` — путь для сохранения обработанного файла
- `target_col` — столбец, с которого начинается удаление (по умолчанию 2)

#### `refactoring_file(name_file)`
📌 Основная функция рефакторинга файла. Выполняет:
- Извлечение данных
- Очистку и перемещение данных
- Удаление ненужных строк
- Объединение и форматирование ячеек

#### `main(name_file)`
📌 Запуск всех функций рефакторинга.

**Аргументы:**
- `name_file` — путь к файлу

---

## 🛠️ Установка и запуск

### 📥 **Требования**
- Python 3.x
- `openpyxl`

### 📌 **Установка зависимостей**
```bash
pip install openpyxl
```

### ▶ **Запуск скрипта**
```bash
python refactoring.py
```

---

## 📂 Структура проекта
```
📁 project_directory
│-- refactoring.py     # Основной код
│-- README.md          # Описание проекта
```

---

## 📌 Пример использования
```python
dir_files = r'C:\Users\Desktop\dir'  # Путь к папке с файлами
```

---

## 📝 Автор
Разработчик: **[Fad5](https://github.com/Fad5)**


